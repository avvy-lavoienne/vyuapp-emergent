import SkillsClient from '@/components/SkillsClient';

export const metadata = {
  title: 'Skill Management',
  description: 'View and manage agent skills across all profiles.',
};

export default function SkillsPage() {
  return <SkillsClient />;
}
